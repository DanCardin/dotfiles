# bindkey -a ' p' skim-file-widget
# bindkey -a ' o' skim-history-widget
# bindkey '^R' skim-history-widget
# bindkey -a ' c' skim-cd-widget

bindkey '^ ' autosuggest-accept
bindkey '^p' backward-word
bindkey '^n' forward-word
