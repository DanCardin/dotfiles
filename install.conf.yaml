- clean: ['~']

- link:
    ~/.vim_session: vim_session
    ~/.vim: vim/
    ~/.vimrc: vimrc

    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases
    ~/.zshrc: zshrc
    ~/.zgen: zgen

    ~/.tmux.conf: tmux.conf

    ~/.virtualenvs: virtualenvs
