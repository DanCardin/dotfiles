- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim: .vim/
    ~/.vimrc: vimrc
    ~/.nvimrc: vimrc
    ~/.bashrc: bashrc
    ~/.bashaliases: bash_aliases

- shell:
  - [git submodule update --init --recursive, Installing submodules]
